services:
  - type: web
    name: sandbox
    env: node
    region: frankfurt
    plan: free
    buildCommand: cd server && npm install
    startCommand: cd server && node index.js
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 10000
